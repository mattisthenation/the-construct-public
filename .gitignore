# Rust build output
/target
# Packaged distribution artifacts (built by the release workflow, not source)
/dist
# Internal build directive — not part of the public product
/CLAUDE.md
# Local databases / state
*.db
*.db-journal
*.db-wal
# Secrets
.env
# macOS
.DS_Store
